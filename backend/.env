# SKOPIA Lens — backend environment variables
# DO NOT commit this file to git (.gitignore must include .env)

# Anthropic API key — required for Helios AI insights
ANTHROPIC_API_KEY=sk-ant-api03-WXdtfdIeyjj...

# Render sets PORT automatically — this is just the local dev fallback
PORT=8000